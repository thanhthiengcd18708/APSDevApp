@model IEnumerable<APSDevApp.Models.Course>
@{
    ViewBag.Title = "Index";
}

<h2> Course Page</h2>

@Html.ActionLink("Create new Course", "Create", "Courses",
    null,
    new { @class = "btn btn-primary" })

@using (Html.BeginForm("Index", "Courses", "POST"))
{
    <div class="form-group">
        <label for="fname">Search by Course Name:</label><br>
        @Html.TextBox("searchString", null, new { @class = "form-control" })
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-primary">Search</button>
    </div>
}

<table class="table table-bordered">
    <thead>
        <tr>
            <th>Course #</th>
            <th>Course Name</th>
            <th>Course Description</th>
            <th>Category Name</th>
            <th>Action</th>

        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Id</td>
                <td>@item.Name</td>
                <td>@item.Description</td>
                <td>@item.Category.Name</td>
                <td>
                    @Html.ActionLink("ViewAssign", "Index", "Assign", new { id = item.Id }, new { @class = "btn btn-primary" })
                    @Html.ActionLink("Details", "Details", "Courses", new { id = item.Id }, new { @class = "btn btn-primary" })
                    @Html.ActionLink("Update", "Update", "Courses", new { id = item.Id }, new { @class = "btn btn-dark" })
                    @Html.ActionLink("Delete", "Delete", "Courses", new { id = item.Id }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </tbody>
</table>